# AI Service для Task Tracker

## Требования

Создайте AI микросервис в этой директории со следующей структурой:

```
ai-service/
├── main.py              # FastAPI приложение с endpoints
├── ai_agent.py          # Логика работы с OpenAI API
├── models.py            # Pydantic модели для request/response
├── requirements.txt     # Python зависимости
├── Dockerfile          # Docker конфигурация
├── .env.example        # Пример переменных окружения
└── README.md           # Документация вашего сервиса
```

## Endpoints которые нужно реализовать

### 1. POST /ai/generate-description
Генерирует описание задачи по её названию

**Request:**
```json
{
  "title": "Реализовать авторизацию"
}
```

**Response:**
```json
{
  "description": "Необходимо разработать систему авторизации..."
}
```

### 2. POST /ai/suggest-deadline
Предлагает дедлайн для задачи

**Request:**
```json
{
  "title": "Исправить критический баг",
  "description": "Баг в продакшене"
}
```

**Response:**
```json
{
  "deadline": "2026-02-02",
  "reasoning": "Критические баги требуют немедленного исправления"
}
```

### 3. POST /ai/analyze-priority
Определяет приоритет задачи

**Request:**
```json
{
  "title": "Добавить темную тему",
  "description": "Для улучшения UX"
}
```

**Response:**
```json
{
  "priority": "low",
  "reasoning": "Улучшение UX, не критично"
}
```

### 4. POST /ai/process-task
Комплексная обработка задачи

**Request:**
```json
{
  "title": "Оптимизировать базу данных"
}
```

**Response:**
```json
{
  "description": "Провести анализ и оптимизацию...",
  "priority": "high",
  "deadline": "2026-02-10",
  "category": "improvement",
  "tags": ["database", "performance"]
}
```

## Важные требования

1. **Порт**: Сервис должен работать на порту **8001**
2. **CORS**: Настройте CORS для работы с frontend (localhost:3000)
3. **OpenAI API**: Используйте переменную окружения `OPENAI_API_KEY`
4. **Обработка ошибок**: Обрабатывайте ситуации, когда OpenAI API недоступен

## Запуск для разработки

```bash
cd ai-service
python -m venv venv
source venv/bin/activate  # или venv\Scripts\activate на Windows
pip install -r requirements.txt
uvicorn main:app --host 0.0.0.0 --port 8001 --reload
```

## Запуск с Docker

```bash
# В корне проекта
docker-compose up --build
```

Удачи! 🚀
